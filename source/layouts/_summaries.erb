<% unless pages.empty? %>
  <% pages.each do |page| %>
    <% author = author(page) %>
    <% show_avatar = !!(defined?(avatars) && !avatars.nil? && avatars && author) %>
    <article class="summary">
      <div class="row">
        <% if show_avatar %>
          <div class="small-2 small-offset-1 columns">
            <a href="<%= page.url %>"><img class="avatar full-width" src="<%= author.avatar %>" alt="<%= author.name %>"></a>
          </div>
        <% end %>
        <div class="small-8<% unless show_avatar %> small-offset-2<% end %> columns">
          <h3 class="summary-title"><a href="<%= page.url %>"><%= title(page) %></a></h3>
          <div class="summary-body">
            <p><%= page.data.summary.strip %>&nbsp; <a class="summary-more" href="<%= page.url %>">Read more&#8230;</a></p>
          </div>
          </p>
        </div>
      </div>
    </article>
  <% end %>
<% end %>
